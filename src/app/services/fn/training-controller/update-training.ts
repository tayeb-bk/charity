/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Training } from '../../models/training';

export interface UpdateTraining$Params {
  id: number;
      body: Training
}

export function updateTraining(http: HttpClient, rootUrl: string, params: UpdateTraining$Params, context?: HttpContext): Observable<StrictHttpResponse<Training>> {
  const rb = new RequestBuilder(rootUrl, updateTraining.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Training>;
    })
  );
}

updateTraining.PATH = '/trainings/put-training/{id}';
